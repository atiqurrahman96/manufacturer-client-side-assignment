import React from 'react';

const Loading = () => {
    return (
        <div className='h-screen flex justify-center items-center'>
            <button className="btn loading text-purple-600">Processing</button>
        </div>
    );
};

export default Loading;